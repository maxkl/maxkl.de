<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8" />
		<title>${data.site.title}</title>
		<meta name="viewport" content="width=device-width,initial-scale=1" />
		<link rel="stylesheet" type="text/css" href="/css/index.css" />
	</head>
	<body>
		<for (section in data.sections)>
			<h2>${section.title}</h2>
			<ul>
				<li for(link in section.links)>
					<a href="${link.href}" target="${link.external ? '_blank' : null}">${link.title}</a>
				</li>
			</ul>
		</for>

		Cookie message:
		<div>Cookies accepted: ${"" + data.cookiesAccepted}</div>
		<include ("./parts/cookieMessage.marko") template-data="{ cookiesAccepted: data.cookiesAccepted }" />
	</body>
</html>